<?xml version="1.0" encoding="UTF-8"?>
<?asciidoc-toc maxdepth="2"?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xl="http://www.w3.org/1999/xlink" version="5.0" xml:lang="en">
<info>
<title>cassandra plugin</title>
<date>2022-12-18</date>
</info>
<chapter xml:id="_cassandra-plugin">
<title>cassandra plugin</title>
<section xml:id="_overview">
<title>Overview</title>
<simpara>The <literal>cassandra</literal> plugin connects QALIPSIS to <link xl:href="https://cassandra.apache.org/_/index.html">APACHE Cassandra</link>.</simpara>
<variablelist>
<varlistentry>
<term>Technologies addressed</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Cassandra: <link xl:href="https://cassandra.apache.org/_/index.html">APACHE Cassandra</link></simpara>
</listitem>
<listitem>
<simpara>Cassandra Query Language: <link xl:href="https://cassandra.apache.org/doc/latest/cassandra/cql/index.html">The Cassandra Query Language (CQL)</link></simpara>
</listitem>
<listitem>
<simpara>DataStax Driver: <link xl:href="https://docs.datastax.com/">DataStax</link>.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Dependency</term>
<listitem>
<simpara><literal>io.qalipsis:plugin-cassandra</literal></simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Namespace in scenario</term>
<listitem>
<simpara><literal>cassandra()</literal></simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Client library</term>
<listitem>
<simpara>DataStax: refer to the <link xl:href="https://docs.datastax.com/">DataStax</link>.</simpara>
<simpara>Datastax is a modern, feature-rich and highly tunable Java client library for Apache Cassandra using Cassandra’s binary protocol and Cassandra Query Language.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Supported steps</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><link linkend="poll step"><literal>poll</literal></link>: polls the newest data periodically.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_save-step"><literal>save</literal></link>: saves records generated from the input and step context.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_search-step"><literal>search</literal></link>: searches records and builds a query from the input and step context.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
<simpara>===Poll step
The <literal>poll</literal> step within the <literal>cassandra</literal> plugin polls the newest records from the database table.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Scenario or step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>The <literal>poll</literal> step is created only once in a scenario; it is then used throughout the scenario as called. The <literal>poll</literal> step within the <literal>cassandra</literal> plugin uses a strategy of “delivered at least once."</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">cassandra().poll {
    connect {
       servers = listOf("localhost:7777", "10.42.42.42:7778")
       keyspace = "test_keyspace"
       datacenterProfile = DriverProfile.LOCAL
       datacenterName = "test_datacenter"
   }
   query("test")
   parameters(listOf(1, "two"))
   tieBreaker {
       name = "name"
       type = GenericType.STRING
   }
    pollDelay(Duration.ofSeconds(1))
    monitoring {
        meters = true
        events = false
   }
   broadcast(123, Duration.ofSeconds(20))
}</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>connect</literal> (required): configures the connection to <link xl:href="https://cassandra.apache.org">APACHE Cassandra</link></simpara>
<itemizedlist>
<listitem>
<simpara><literal>keyspace</literal>(required): the outermost object that determines how data replicates on nodes.</simpara>
</listitem>
<listitem>
<simpara><literal>datacenterProfile</literal>(required): an execution profile that encapsulates a group of settings associated with individual queries.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>query</literal> (required): defines the prepared statement to execute when polling.</simpara>
</listitem>
<listitem>
<simpara><literal>parameters</literal> (optional): binds values to the clauses of the prepared query.</simpara>
</listitem>
<listitem>
<simpara><literal>tieBreaker</literal> (required): repeats the first column used for sorting and comparison (<literal>&#8656;</literal> or <literal>&gt;=</literal>, depending on the ascending or descending order).</simpara>
</listitem>
<listitem>
<simpara><literal>pollDelay</literal> (required): specifies the delay between query executions.</simpara>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
<listitem>
<simpara><literal>broadcast</literal> (optional): specifies the broadcast parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tips</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Use the <literal>poll</literal> step with a left join operator to verify the values saved by your tested system in the database.</simpara>
</listitem>
<listitem>
<simpara><literal>query</literal> requires at least one sorting field (<literal>tiebreaker</literal> in the example) to filter the newest results and not fetch the same records repeatedly.</simpara>
</listitem>
<listitem>
<simpara>The latest known value for <literal>tiebreaker</literal> is used to filter out values already received from the database.</simpara>
</listitem>
<listitem>
<simpara>Refer to the <link xl:href="https://cassandra.apache.org/doc/latest">APACHE Cassandra</link> documentation further parameter and configuration information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_save-step">
<title>Save step</title>
<simpara>The <literal>save</literal> step within the <literal>cassandra</literal> plugin persists records into a cassandra database using the save query with Cassandra Query Language (CQL).</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>The <literal>save</literal> step&#8217;s input and step context are used to generate documents to save into a table.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered"> cassandra().save {
    connect {
       servers = listOf("localhost:7777", "10.42.42.42:7778")
       keyspace = "test_keyspace"
       datacenterProfile = DriverProfile.LOCAL
       datacenterName = "test_datacenter"
   }
    tableName = tableName
    columnsConfig = columns
    rowsFactory = rowsFactory
    monitoring {
        meters = true
        events = false
   }
}</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>connect</literal> (required): configures the connection to <link xl:href="https://cassandra.apache.org">APACHE Cassandra</link>.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>keyspace</literal>(required): the outermost object that determines how data replicates on nodes.</simpara>
</listitem>
<listitem>
<simpara><literal>datacenterProfile</literal>(required): an execution profile that encapsulates a group of settings associated with individual queries.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tips</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Use the <literal>save</literal> after a <literal>collect</literal> step in order to reduce the number of queries executed onto the database and reduce the load your scenario generates on it.</simpara>
</listitem>
<listitem>
<simpara>The <literal>save</literal> step is extremely flexible; table, rows, and columns depend on the context and input value received from the previous step.</simpara>
</listitem>
<listitem>
<simpara>Refer to the <link xl:href="https://cassandra.apache.org/doc/latest">APACHE Cassandra</link> documentation for further parameter and configuration information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_search-step">
<title>Search step</title>
<simpara>The <literal>search</literal> step within the <literal>cassandra</literal> plugin searches records in the database using a search query.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>The <literal>search</literal> step’s input and step context are used to complete the search; the list of results is then forwarded to the next step(s).</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">cassandra().search {
connect {
       servers = listOf("localhost:7777", "10.42.42.42:7778")
       keyspace = "test_keyspace"
       datacenterProfile = DriverProfile.LOCAL
       datacenterName = "test_datacenter"
}
   query("test")
   parameters(listOf(1, "two"))
   tieBreaker {
       name = "name"
       type = GenericType.STRING
    }
    monitoring {
        meters = true
        events = false
   }
}</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>connect</literal> (required): configures the connection to  <link xl:href="https://cassandra.apache.org">APACHE Cassandra</link>.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>keyspace</literal>(required): the outermost object that determines how data replicates on nodes.</simpara>
</listitem>
<listitem>
<simpara><literal>datacenterProfile</literal>(required): an execution profile that encapsulates a group of settings associated with individual queries.</simpara>
</listitem>
<listitem>
<simpara><literal>query</literal> (required): defines the prepared statement to execute when searching.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>parameters</literal>: (optional): binds values to the clauses of the prepared query.</simpara>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tips</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Use the <literal>search</literal> step after a <literal>collect()</literal> step to reduce the number of queries executed onto the database and to reduce the load your scenario generates on the test system.</simpara>
</listitem>
<listitem>
<simpara>The <literal>query</literal> Parameter may contain ordering clauses.</simpara>
</listitem>
<listitem>
<simpara>Refer to the <link xl:href="https://cassandra.apache.org/doc/latest">APACHE Cassandra</link> documentation for further parameter and configuration information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
</chapter>
</book>