<?xml version="1.0" encoding="UTF-8"?>
<?asciidoc-toc maxdepth="2"?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xl="http://www.w3.org/1999/xlink" version="5.0" xml:lang="en">
<info>
<title>netty plugin</title>
<date>2022-12-18</date>
</info>
<chapter xml:id="_netty-plugin">
<title>netty plugin</title>
<section xml:id="_overview">
<title>Overview</title>
<simpara>The <link xl:href="https://netty.io/">netty</link> plugin supports <link xl:href="https://datatracker.ietf.org/doc/html/rfc793">TCP</link>, <link xl:href="https://datatracker.ietf.org/doc/html/rfc768">UDP</link>, <link xl:href="https://httpwg.org/">HTTP</link> and <link xl:href="https://mqtt.org/">MQTT</link> clients.</simpara>
<variablelist>
<varlistentry>
<term>Technologies addressed</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>HTTP/1.1: <link xl:href="https://httpwg.org/">https://httpwg.org/</link></simpara>
</listitem>
<listitem>
<simpara>HTTP/2: <link xl:href="https://httpwg.org/">https://httpwg.org/</link></simpara>
</listitem>
<listitem>
<simpara>MQTT: <link xl:href="https://mqtt.org/">https://mqtt.org/</link></simpara>
</listitem>
<listitem>
<simpara>TCP (Transmission Control Protocol): <link xl:href="https://datatracker.ietf.org/doc/html/rfc793">https://datatracker.ietf.org/doc/html/rfc793</link></simpara>
</listitem>
<listitem>
<simpara>UDP (User Datagram Protocol): <link xl:href="https://datatracker.ietf.org/doc/html/rfc768">https://datatracker.ietf.org/doc/html/rfc768</link></simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Dependency</term>
<listitem>
<simpara><literal>io.qalipsis:plugin-netty</literal></simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Namespace in scenario</term>
<listitem>
<simpara><literal>netty()</literal></simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Client library</term>
<listitem>
<simpara>Netty: refer to the <link xl:href="https://netty.io/">Netty</link> project.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Supported steps</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><link linkend="_http-step"><literal>HTTP</literal></link>: executes an HTTP request using <link xl:href="https://datatracker.ietf.org/doc/rfc2616"><literal>HTTP 1.1</literal></link> or <link xl:href="https://datatracker.ietf.org/doc/rfc7540"><literal>HTTP 2.0</literal></link>.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_http-with-step"><literal>HTTP WITH</literal></link>: executes an HTTP request reusing the HTTP connection created by a precedent step.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_close-http-step"><literal>CLOSE HTTP</literal></link>: closes an HTTP connection created by a precedent step.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_mqtt-publish-step"><literal>MQTT Publish</literal></link>: sends data to an MQTT server.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_mqtt-subscribe-step"><literal>MQTT Subscribe</literal></link>: consumes data from an MQTT server.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_tcp-step"><literal>TCP</literal></link>: exchanges data with a TCP server.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_tcp-with-step"><literal>TCP WITH</literal></link>: exchanges data reusing the TCP connection created by a precedent step.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_close-tcp-step"><literal>CLOSE TCP</literal></link>: closes a TCP connection created by a precedent step.</simpara>
</listitem>
<listitem>
<simpara><link linkend="_udp-step"><literal>UDP</literal></link>: exchanges data with a UDP endpoint.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_http-step">
<title>HTTP step</title>
<simpara>The <literal>HTTP</literal> step within the <literal>netty</literal> plugin executes an HTTP request using <link xl:href="https://datatracker.ietf.org/doc/rfc2616"><literal>HTTP 1.1</literal></link> or <link xl:href="https://datatracker.ietf.org/doc/rfc7540"><literal>HTTP 2.0</literal></link>.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Scenario and Step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>The <literal>HTTP</literal> step establishes a connection with the server using either a pool of connections or an individual connection for each minion.</simpara>
</listitem>
<listitem>
<simpara>If an individual connection is used, the connection is maintained until the latest execution for the minion is performed, including the ones from the related <link linkend="_http-with-step"><literal>HTTP WITH</literal></link> step, as in the example.</simpara>
</listitem>
<listitem>
<simpara>When the step is kept open without limit, a <link linkend="_close-tcp-step"><literal>CLOSE TCP</literal></link> step must be executed to properly close the connections and avoid saturation of the system.</simpara>
<simpara>Be aware that the number of open connections that can be concurrently maintained depends on the configuration of your operating system. If there are issues with maintaining open connections, try adapting the limits of the file descriptors.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">netty().http {
   request { context, input -&gt; SimpleHttpRequest(HttpMethod.HEAD, "/head") }
   connect {
       url("https://my-website.com:8080/test")
       connectTimeout = Duration.ofSeconds(5)
       inflate()
       charset(Charsets.ISO_8859_1)
       maxContentLength(123)
       followRedirections(15)
       noDelay = false
       keepConnectionAlive = false
       tls {
           disableCertificateVerification = true
       }
       proxy {
           type = HttpProxyType.SOCKS5
           address("my-proxy", 9876)
       }
       followRedirections(5)
   }
   pool {
       size = 143
       checkHealthBeforeUse = true
   }
   monitoring {
       events = true
       meters = true
   }
}.deserialize(String::class)</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>request</literal>: a factory to create the request to exchange, using the step context and the input as arguments.</simpara>
</listitem>
<listitem>
<simpara><literal>connect</literal>: configures the connection to the remote endpoint.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>version</literal>: version of the HTTP protocol to use: <literal>HTTP_1_1</literal> or <literal>HTTP_2_0</literal>,; defaults to <literal>HTTP_1_1</literal>.</simpara>
</listitem>
<listitem>
<simpara><literal>url</literal>: the root URL, the path of the request is appended to it.</simpara>
</listitem>
<listitem>
<simpara><literal>connectTimeout</literal>: limit duration to fully establish the connection; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>readTimeout</literal>: limit duration to receive a response and read it totally after a request; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>shutdownTimeout</literal>: limit duration to close a connection; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>sendBufferSize</literal>: the size of the buffer used to prepare the data to send; defaults to 1024 bytes.</simpara>
</listitem>
<listitem>
<simpara><literal>receiveBufferSize</literal>: the size of the buffer to receive the data of the response; defaults to 1024 bytes.</simpara>
</listitem>
<listitem>
<simpara><literal>nettyChannelOptions</literal>: overwrites the options of the <literal>netty</literal> channel, use with care.</simpara>
</listitem>
<listitem>
<simpara><literal>noDelay</literal>: disables the Nagle Algorithm; defaults to true.</simpara>
</listitem>
<listitem>
<simpara><literal>keepConnectionAlive</literal>: when set to true, the HTTP header Connection is set to Keep-Alive and the client connection is kept open until <literal>CLOSEHTTP</literal>() is called.</simpara>
</listitem>
<listitem>
<simpara><literal>tls</literal>: configuration for SSL/TLS</simpara>
<itemizedlist>
<listitem>
<simpara><literal>disableCertificationVerification</literal>: set it to true when the certificate on server should not be verified (for example for a self-signed certificate)</simpara>
</listitem>
<listitem>
<simpara><literal>protocols</literal>: supported protocols; defaults to the <literal>netty</literal> default for the related HTTP version.</simpara>
</listitem>
<listitem>
<simpara><literal>ciphers</literal>: supported cipher algorithms; defaults to the netty` default for the related HTTP version.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>proxy</literal>: configuration to exchange HTTP data via a proxy; defaults to no configuration.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>address</literal>: the host and port of the proxy.</simpara>
</listitem>
<listitem>
<simpara><literal>type</literal>: type of the proxy to use: <literal>HTTP</literal>, <literal>SOCKS5</literal>; defaults to <literal>HTTP</literal>.</simpara>
</listitem>
<listitem>
<simpara><literal>authenticate</literal>: username and optional password to authenticate to the proxy, if it supports and requires it.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>charset</literal>: character encoding of the HTTP requests,</simpara>
</listitem>
<listitem>
<simpara><literal>maxContentLength</literal>: maximal expected size of a request, in bytes, defaults to <literal>1048576</literal>, which is the minimum allowed.</simpara>
</listitem>
<listitem>
<simpara><literal>inflate</literal>: enables the acceptance of compressed response. Compression of the request is not supported.</simpara>
</listitem>
<listitem>
<simpara><literal>followRedirections</literal>: when the server returns an HTTP status 3xx (redirection) in the response, the client will resend the request to the provided location. Otherwise, the response is kept as is. The argument maxRedirections provides the maximal number of redirections to follow; defaults to 10. By default, the redirections are not followed at all. Note that following a redirections makes the request to be sent there, exactly as it is (same method and content).</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>pool</literal>: configuration of the pool of connections; the <literal>pool</literal> is disabled by default.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>size</literal>: number of constant connections maintained in the pool.</simpara>
</listitem>
<listitem>
<simpara><literal>checkHealthBeforeUse</literal>: verifies the health of the connection when polled from the <literal>pool</literal> and before it is used.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
<listitem>
<simpara><literal>deserialize</literal>: specifies the class to use to deserialize the body of the response.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Refer to the <link xl:href="https://netty.io/wiki/index.html">Netty</link> documentation for further configuration and parameter information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_http-with-step">
<title>HTTP WITH step</title>
<simpara>The <literal>HTTP WITH</literal> step within the <literal>netty</literal> plugin executes an HTTP request reusing the HTTP connection created by a precedent step.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>The <literal>HTTP WITH</literal> step is a direct or indirect follower of an <literal>HTTP</literal> step. <literal>HTTP WITH</literal> specifies the name of the step that owns the connection, and reuses a connection from that step as it was created (either from a pool or the connection attached to the minion).</simpara>
</listitem>
<listitem>
<simpara><literal>HTTP WITH</literal> cannot configure the connection differently than the <literal>HTTP</literal> step it relates to.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
</listitem>
</varlistentry>
</variablelist>
<programlisting language="kotlin" linenumbering="numbered">netty().httpWith("name-of-a-previous-HTTP-step") {
   request { context, input -&gt; SimpleHttpRequest(HttpMethod.HEAD, "/head") }
   monitoring { all() }
}.deserialize(String::class)</programlisting>
<variablelist>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>netty().httpWith</literal>("name-of-a-previous-HTTP-step"): the name of the <literal>HTTP</literal> step that owns the connection.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>request</literal>: a factory to create the request to exchange, using the step context and the input as arguments.</simpara>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>deserialize</literal>: specifies the class to use to deserialize the body of the response.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Refer to the <link xl:href="https://netty.io/wiki/index.html">Netty</link> documentation for further configuration and parameter information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_close-http-step">
<title>CLOSE HTTP step</title>
<simpara>The <literal>CLOSE HTTP</literal> step within the <literal>netty</literal> plugin closes the <literal>HTTP</literal> connection attached to the minion, and created by a direct or indirect ancestor step, when the tail of the minion workflow is received.</simpara>
<simpara>It is always a best practice to use a <emphasis>CLOSE HTTP</emphasis>  step when reusing an <literal>HTTP</literal> connection across several steps without a pool of connections.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>The <literal>CLOSE HTTP</literal> step is a direct or indirect follower of an <literal>HTTP</literal> step which keeps the connection open.  <literal>CLOSE HTTP</literal> simply forwards the input it receives to the next step(s).</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
</listitem>
</varlistentry>
</variablelist>
<programlisting language="kotlin" linenumbering="numbered">netty().closeHttp("name-of-a-previous-HTTP-step")</programlisting>
<variablelist>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>closeHttp</literal> (required)  specifies the  name of an <literal>HTTP</literal> step owning the <literal>HTTP</literal> connection to close.</simpara>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Refer to the <link xl:href="https://netty.io/wiki/index.html">Netty</link> documentation for further configuration and parameter information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_mqtt-publish-step">
<title>MQTT publish step</title>
<simpara>The <literal>MQTT Publish</literal> step within the <literal>netty</literal> plugin sends data to an MQTT server.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>To be provided.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>To be provided.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">To be provided.</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<simpara>To be provided.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip(s)</term>
<listitem>
<simpara>To be provided.</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_mqtt-subscribe-step">
<title>MQTT subscribe step</title>
<itemizedlist>
<listitem>
<simpara>The <literal>MQTT Subscribe Step</literal> within the <literal>netty</literal> plugin consumes data from an MQTT server.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>To be provided.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>To be provided.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">To be provided.</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable Parameters</term>
<listitem>
<simpara>To be provided.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip(s)</term>
<listitem>
<simpara>To be provided</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
</itemizedlist>
</section>
<section xml:id="_tcp-step">
<title>TCP step</title>
<simpara>The <literal>TCP</literal> step within the <literal>netty</literal> plugin is a client for <literal>TCP</literal>.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Scenario and Step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>The TCP step establishes a connection with the server using either a pool of connections or an individual connection for each minion.</simpara>
<simpara>If an individual connection is used, the connection is maintained until the latest execution for the minion is performed, including the ones from the related <link linkend="TCP WITH"><literal>TCP WITH</literal></link> step, as in the example.</simpara>
<simpara>When the step is kept open without limit, a <link linkend="CLOSE TCP Step"><literal>CLOSE TCP</literal></link> step should be executed to properly close the connections and avoid a saturation of the system.</simpara>
<simpara>Be aware that the number of open connections that can be concurrently maintained depends on the configuration of your operating system. If there are issues with maintaining open connections, adapting the limits for file descriptors may help.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">netty().tcp {
   request { context, input -&gt; input.encodeToByteArray() }
   connect {
        address("remote-server.acme.com", 9000)
        connectTimeout = Duration.ofMinutes(1)
        noDelay = false
        keepConnectionAlive = false
        tls {
            disableCertificateVerification = true
        }
        proxy {
            type = HttpProxyType.SOCKS5
            address("my-proxy", 9876)
        }
    }
    pool {
       size = 1000
       checkHealthBeforeUse = true
    }
    monitoring {
       events = true
       meters = true
   }
}</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>request</literal>: a factory to create the request to exchange, using the step context and the input as arguments; the request is an array of bytes.</simpara>
</listitem>
<listitem>
<simpara><literal>connect</literal>:configures the connection to the remote endpoint.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>address</literal>:hostname or IP address and port of the remote server.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>connectTimeout</literal>: limit duration to fully establish the connection; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>readTimeout</literal>:limit duration to receive and read a response after a request; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>shutdownTimeout</literal>:limit duration to close a connection; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>sendBufferSize</literal>: the size of the buffer used to prepare the data to send; defaults to 1024 bytes.</simpara>
</listitem>
<listitem>
<simpara><literal>receiveBufferSize</literal>: the size of the buffer to receive the data of the response; defaults to 1024 bytes.</simpara>
</listitem>
<listitem>
<simpara><literal>nettyChannelOptions</literal>: overwrites the options of the <literal>netty</literal> channel, use with care.</simpara>
</listitem>
<listitem>
<simpara><literal>noDelay</literal>: disables the Nagle Algorithm; defaults to <literal>true</literal>.</simpara>
</listitem>
<listitem>
<simpara><literal>tls</literal>: configuration for SSL/TLS</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>disableCertificationVerification</literal>: set to true if the certificate on the server should not be verified (for example for a self-signed certificate).</simpara>
</listitem>
<listitem>
<simpara><literal>protocols</literal>: supported protocols; defaults to the <literal>netty</literal> default.</simpara>
</listitem>
<listitem>
<simpara><literal>ciphers</literal>: supported cipher algorithms; defaults to the <literal>netty</literal> default.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>proxy</literal>: configuration to exchange data via a proxy; defaults to no configuration.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>address</literal>: hostname or IP address and port of the proxy server.</simpara>
</listitem>
<listitem>
<simpara><literal>type</literal>: type of the proxy to use: <literal>SOCKS4</literal>, <literal>SOCKS5</literal>; defaults to <literal>SOCKS4</literal>.</simpara>
</listitem>
<listitem>
<simpara><literal>authenticate</literal>: username and optional password to authenticate to the proxy, if the proxy supports and requires it.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>pool</literal>: configuration of the pool of connections; defaults to disabled.</simpara>
<itemizedlist>
<listitem>
<simpara><literal>size</literal>: number of constant connections maintained in the pool.</simpara>
</listitem>
<listitem>
<simpara><literal>checkHealthBeforeUse</literal>: verifies the health of the connection when polled from the pool and before it is used.</simpara>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Refer to the <link xl:href="https://netty.io/wiki/index.html">Netty</link> documentation for further configuration and parameter information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_tcp-with-step">
<title>TCP WITH step</title>
<simpara>The <literal>TCP WITH step</literal> within the <literal>netty</literal> plugin sends a TCP payload, reusing the connection created by a precedent step.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>The <literal>TCP WITH</literal> step is a direct or indirect follower of a <literal>TCP</literal>  step. <literal>TCP WITH</literal> specifies the name of the step that owns the connection, and reuses a connection from that step  as it was created (either from a pool or the connection attached to the minion).</simpara>
</listitem>
<listitem>
<simpara><literal>TCP WITH</literal> cannot configure the connection differently than the <literal>TCP</literal> step it relates to.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">netty().tcpWith("name-of-a-previous-HTTP-step") {
   request { context, input -&gt; input.encodeToByteArray() }
   monitoring { all() }
}</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>request</literal>: a factory to create the request to exchange, using the step context and the input as arguments.</simpara>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Refer to the <link xl:href="https://netty.io/wiki/index.html">Netty</link> documentation for further configuration and parameter information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_close-tcp-step">
<title>CLOSE TCP step</title>
<simpara>The <literal>CLOSE TCP</literal> step within the <literal>netty</literal> plugin closes the TCP connection, attached to a minion and created by a direct or indirect ancestor step, when the tail of the minion workflow is received.</simpara>
<simpara>It is best practice to use a <literal>CLOSE TCP</literal> step when reusing a <literal>TCP</literal> connection across several steps without a pool of connections.</simpara>
<variablelist>
<varlistentry>
<term>Ancestor</term>
<listitem>
<simpara>Step</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>The <literal>CLOSE TCP</literal> step is a direct or indirect follower of an <literal>HTTP</literal> step which keeps the connection open. <literal>CLOSE TCP</literal> simply forwards the input it receives to the next step(s).</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">netty().closeHttp("name-of-a-previous-HTTP-step")</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>closeHttp</literal> (required):  specifies the  name of the <literal>HTTP</literal> step to close.</simpara>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Refer to the <link xl:href="https://netty.io/wiki/index.html">Netty</link> documentation for further configuration and parameter information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
<section xml:id="_udp-step">
<title>UDP step</title>
<simpara>The <literal>UDP</literal> step within the <literal>netty</literal> plugin is a client for UDP.</simpara>
<simpara>It is best practice to use the  <literal>UDP</literal> step when reusing a <literal>TCP</literal> connection across several steps without a pool of connections.</simpara>
<simpara>Ancestors:
Scenario and Step</simpara>
<variablelist>
<varlistentry>
<term>Functionality</term>
<listitem>
<simpara>The <literal>UDP</literal> step creates a UDP Datagram packet from the <literal>request</literal> and sends it to the remote endpoint and expects a response.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>Example</term>
<listitem>
<programlisting language="kotlin" linenumbering="numbered">netty().udp {
    request { context, input -&gt; input.encodeToByteArray() }
    connect {
       address("remote-server.acme.com", 9000)
       connectTimeout = Duration.ofMinutes(1)
    }
    pool {
       size = 143
       checkHealthBeforeUse = true
   }
   monitoring {
       events = true
       meters = true
   }
}</programlisting>
</listitem>
</varlistentry>
<varlistentry>
<term>Notable parameters</term>
<listitem>
<itemizedlist>
<listitem>
<simpara><literal>request</literal>: a factory to create the request to exchange, using the step context and the input as arguments, the request is an array of bytes</simpara>
</listitem>
<listitem>
<simpara><literal>connect</literal>: configures the connection to the remote endpoint</simpara>
<itemizedlist>
<listitem>
<simpara><literal>address</literal>: hostname or IP address and port of the remote endpoint</simpara>
</listitem>
<listitem>
<simpara><literal>connectTimeout</literal>: limit duration to fully establish the connection; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>readTimeout</literal>: limit duration to receive a response and read it totally after a request; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>shutdownTimeout</literal>: limit duration to close a connection; defaults to 10 seconds.</simpara>
</listitem>
<listitem>
<simpara><literal>sendBufferSize</literal>: the size of the buffer used to prepare the data to send; defaults to <literal>1024</literal> bytes.</simpara>
</listitem>
<listitem>
<simpara><literal>receiveBufferSize</literal>: the size of the buffer to receive the data of the response; defaults to <literal>1024</literal> bytes.</simpara>
</listitem>
<listitem>
<simpara><literal>nettyChannelOptions</literal>: overwrites the options of the <literal>netty</literal> channel, use with care.</simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>monitoring</literal>(optional): configures the monitoring parameters for the step.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
<varlistentry>
<term>Tip</term>
<listitem>
<itemizedlist>
<listitem>
<simpara>Refer to the <link xl:href="https://netty.io/wiki/index.html">Netty</link> documentation for further configuration and parameter information.</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
</section>
</chapter>
</book>