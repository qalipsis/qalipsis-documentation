<?xml version="1.0" encoding="UTF-8"?>
<?asciidoc-toc maxdepth="2"?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xl="http://www.w3.org/1999/xlink" version="5.0" xml:lang="en">
<info>
<title>QALIPSIS configuration</title>
<date>2023-04-06</date>
</info>
<chapter xml:id="_starting-qalipsis-in-different-modes">
<title>Starting QALIPSIS in different modes</title>
<simpara>Two modes of QALIPSIS configuration are currently supported:</simpara>
<itemizedlist>
<listitem>
<simpara>Interactive</simpara>
</listitem>
<listitem>
<simpara>Scenario Selection</simpara>
</listitem>
</itemizedlist>
<section xml:id="_interactive">
<title>Interactive</title>
<orderedlist numeration="arabic">
<listitem>
<simpara>Execute QALIPSIS with the option <literal>--prompt</literal> or <literal>-p</literal>.
as in the following example:</simpara>
<programlisting language="kotlin" linenumbering="unnumbered">java -jar my-scenario-qalipsis.jar --prompt</programlisting>
</listitem>
<listitem>
<simpara>When prompted, enter the load factor (# of minions), speed factor, ramping strategy, etc.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="_scenario-selection">
<title>Scenario selection</title>
<simpara>Execute QALIPSIS with the <literal>--scenarios</literal> or <literal>-s</literal> as in the following example:</simpara>
<programlisting language="kotlin" linenumbering="unnumbered">java -jar my-scenario-qalipsis.jar --scenarios "my-first-scenario, ?y-*-s*n?rio"</programlisting>
<simpara>The value is a comma-separated list of an exact value of a scenario, a pattern of the scenario, or both. The pattern may contain wildcards (* or ?) as in the example above.</simpara>
</section>
</chapter>
<chapter xml:id="_runtime-configuration">
<title>Runtime configuration</title>
<section xml:id="_command-line-arguments">
<title>Command-line arguments</title>
<simpara>This section provides an overview and an example of the command-line arguments that are available at runtime.</simpara>
<simpara>Note that all command-line arguments can be provided in a file, as explained in <xref linkend="_external-property-sources"/>.</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Autostart mode.
The <literal>--autostart</literal> argument automatically executes all the scenarios present in the class path, when QALIPSIS starts.</simpara>
</listitem>
<listitem>
<simpara>Number of Minions by Scenario.</simpara>
<simpara>This command defines how many minions should be used in the load-injected tree. It is not compliant with <literal>campaign.minions-factor</literal>.</simpara>
<simpara>Following is an example of the command to use to set the number of minions:</simpara>
<programlisting language="kotlin" linenumbering="unnumbered">---
campaign.minions-count-per-scenario (#ofMinions)
---</programlisting>
<simpara>where <literal>#ofMinions</literal> is an integer and the default value is the <literal>minionCount</literal> set during configuration of the scenario.</simpara>
</listitem>
<listitem>
<simpara><literal>Load Factor</literal>.</simpara>
<simpara>The <literal>load factor</literal> command multiplies the number of minions set in the scenario specification by the provided value. It is not compliant with <literal>campaign.minions-count-per-scenario</literal>.</simpara>
<itemizedlist>
<listitem>
<simpara>If the value is set between 0 and 1, the <literal>minionCount</literal> is reduced.</simpara>
</listitem>
<listitem>
<simpara>If the value is set to greater than one (or <literal>double</literal>,<literal>triple</literal>, <literal>quadruple</literal>, etc.) the <literal>minionCount</literal> is increased.</simpara>
</listitem>
<listitem>
<simpara>The default value is always set to <literal>1</literal>.</simpara>
<simpara>Following is an example of the command to set the load factor:</simpara>
<simpara><literal>campaign.minions-factor, double, default to 1</literal></simpara>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<simpara><literal>Speed Factor</literal>.
The speed factor command is applied on the ramping strategy to accelerate or slow down the pace of the minions released.</simpara>
<simpara>For example:</simpara>
<simpara><literal>campaign.speed-factor, double, default to 1</literal></simpara>
<simpara>The value for the speed factor is defined the same as for load factor.</simpara>
</listitem>
</orderedlist>
</section>
<section xml:id="_external-property-sources">
<title>External property sources</title>
<simpara>QALIPSIS uses Micronaut’s runtime underneath. Hence, you can use its mechanism of <link xl:href="https://docs.micronaut.io/latest/guide/index.html#propertySource">Externalized Configuration with Property Sources</link>.</simpara>
<simpara>The configuration files contain all of the settings that are relevant for QALIPSIS to run. Included in the settings the parameters for logging and monitoring data exports, data sources for the Head node, executor sizes, etc.</simpara>
<simpara>You can overwrite the default Qalipsis configuration and include your own properties by creating a file named qalipsis.{extension} or config/qalipsis.{extension} in the classpath or in the working directory or both. The {extension} can be either of the following:</simpara>
<itemizedlist>
<listitem>
<simpara>YAML documents (use the extension yaml or yml)</simpara>
</listitem>
<listitem>
<simpara>JSON documents (use the extension json)</simpara>
</listitem>
<listitem>
<simpara>flat properties (use the extension properties)</simpara>
</listitem>
</itemizedlist>
<simpara>Yaml or yml files have more precedence over a json file and json files have more precedence over a properties file. In a situation, when we have two YAML files with the same name and located in the same directory, the file with the yaml extension has precedence over the one with yml extension.</simpara>
<simpara>You can specify the same parameters in different environment sources, however the levels of precedence of the configuration sources determines the configuration values that are prioritized.</simpara>
<simpara>The precedence of the configuration sources, from lowest to highest levels is outlined below:</simpara>
<itemizedlist>
<listitem>
<simpara>Default configuration embedded in QALIPSIS artifacts</simpara>
</listitem>
<listitem>
<simpara>Class path files:  the files in /config have precedence over those in the root</simpara>
</listitem>
<listitem>
<simpara>Working directory files: the files in /config have precedence over those in the root -OS Environment variables</simpara>
<itemizedlist>
<listitem>
<simpara>Java System properties(-DmyArg=value)</simpara>
</listitem>
<listitem>
<simpara>Command line arguments (using -c “myArg=value”).</simpara>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>
</section>
</chapter>
</book>